plugins {
    id 'java'
}

jar {
    archiveName = "${project.name}-${project.version}.jar"
    manifest {
        attributes 'Main-Class': 'agent.demo.test.Test'
    }
}


sourceCompatibility = 1.8

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
